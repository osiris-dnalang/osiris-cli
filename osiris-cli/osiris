#!/usr/bin/env bash
# OSIRIS wrapper script - no external telemetry, local SDK only
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="${PY:-python3}"
exec "$PY" "$SCRIPT_DIR/osiris_cli.py" "$@"
